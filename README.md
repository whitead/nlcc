# nlcc

To turn on automatic context detextion, use

nlcc --auto_context

To change the Codex search temperature to TEMP, run
temperature TEMP
or
codex-temp TEMP

`c` resets context

## key bindings
* `ctrl-t` copy